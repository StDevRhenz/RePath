# RePath
Digital Bureaucracy Recovery Agent - an autonomous agent that takes a failed/rejected document-heavy process, diagnoses what went wrong, creates a recovery plan, performs the actions it safely can, requests missing human input, resumes the workflow, and verifies that the case is ready for resubmission.
